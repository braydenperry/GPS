namespace GPS.Data
{
    /// <summary>
    /// Enum for the return values of ValidatePredicted.
    /// </summary>
    public enum RetVal
    {
        validWithEndTime = 1,
        validNoEndTime = 2,
        invalid = 3
    }
}